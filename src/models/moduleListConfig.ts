import { ThemeColor } from './Theme';

export interface ModuleListConfiguration {
  themeColor: ThemeColor
  bannerActivated: boolean
  bannerEdit: boolean
  leftArrow: boolean
  rightArrow: boolean
  editElements: boolean
  carrousel: boolean
}